[build]
builder = "nixpacks"

[build.env]
NIXPACKS_NO_CACHE = "1"
PIP_NO_CACHE_DIR = "1"
PYTHON_VERSION = "3.10"
RANDOM_BUILD_SEED = "RICE_PROD_REVISION_6"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
healthCheckPath = "/health"
healthCheckTimeout = 120
restartPolicyType = "ON_FAILURE"
